{
  "name": "saf-mcp-tpa-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "echo 'No tests yet'",
    "lint": "echo 'Use ESLint locally'",
    "format": "echo 'Use Prettier locally'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.4.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.4"
  }
}
